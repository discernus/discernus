name: CI - Development Velocity with Python 3.13 Support

# Trigger on pushes and pull requests to maintain development velocity
on:
  push:
    branches: [ main, develop, 'feature/*', 'fix/*', 'codex/*' ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false  # Continue testing other Python versions if one fails
      matrix:
        python-version: ['3.11', '3.13']
        include:
          - python-version: '3.11'
            label: 'stable'
            required: true
          - python-version: '3.13'
            label: 'future'
            required: false  # Allow 3.13 to fail without blocking PRs initially
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        allow-prereleases: true  # Important for Python 3.13
    
    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-py${{ matrix.python-version }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-py${{ matrix.python-version }}-pip-
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r tests/test_requirements.txt
    
    - name: Run unit tests
      run: |
        echo "🧪 Running unit tests on Python ${{ matrix.python-version }} (Phase 1 CI)..."
        python3 -m pytest tests/unit/ -v --tb=short --maxfail=5
    
    - name: Basic import validation
      run: |
        echo "🔍 Validating core imports on Python ${{ matrix.python-version }}..."
        python3 -c "
        try:
            import src.utils.cost_manager
            import src.models.base
            import src.coordinate_engine
            print('✅ Core imports successful on Python ${{ matrix.python-version }}')
        except ImportError as e:
            print(f'❌ Import error on Python ${{ matrix.python-version }}: {e}')
            exit(1)
        "

    - name: Python 3.13 specific tests
      if: matrix.python-version == '3.13'
      run: |
        echo "🚀 Running Python 3.13 specific validation..."
        python3 -c "
        import sys
        print(f'✅ Python version: {sys.version}')
        
        # Test new 3.13 features compatibility
        try:
            # Test that our codebase works with 3.13's improved error messages
            import src.analysis.statistics
            print('✅ Framework Specification v3.1 compatible with Python 3.13')
            
            # Test scientific computing stack
            import numpy as np
            import pandas as pd
            import scipy as sp
            print(f'✅ Scientific stack: NumPy {np.__version__}, Pandas {pd.__version__}, SciPy {sp.__version__}')
            
            # Test LLM clients 
            import openai
            import anthropic
            print('✅ LLM API clients compatible with Python 3.13')
            
        except Exception as e:
            print(f'❌ Python 3.13 compatibility issue: {e}')
            exit(1)
        "
    
    - name: License compliance check
      # Only run on Python 3.11 to avoid duplicate license checks
      if: matrix.python-version == '3.11'
      run: |
        echo "📋 Checking license compliance..."
        cd product_management/license_audit
        echo "🔍 Installing pip-licenses for comprehensive scanning..."
        pip install pip-licenses
        echo "🎯 Running STRICT SaaS/Commercial license compliance check..."
        if python3 compliance_checker.py --policy-file saas_commercial_policy.json; then
          echo "✅ License compliance validated - Safe for SaaS/Commercial deployment"
        else
          echo "🚨 CRITICAL: License compliance FAILED"
          echo "❌ Deployment BLOCKED due to license violations"
          echo "🔧 Action required: Fix license issues before merging"
          echo ""
          echo "📋 License compliance report:"
          python3 compliance_checker.py --policy-file saas_commercial_policy.json --output-format markdown || true
          exit 1
        fi
        echo "💾 Generating license audit trail..."
        python3 run_audit.py --output-dir ci_audit_$(date +%Y%m%d_%H%M%S) --policy-file saas_commercial_policy.json
    
    - name: Test configuration validation
      run: |
        echo "⚙️ Validating test configuration on Python ${{ matrix.python-version }}..."
        python3 -c "
        import os
        import sys
        
        # Check test structure
        test_dirs = ['tests/unit', 'tests/integration', 'tests/utilities']
        for test_dir in test_dirs:
            if not os.path.exists(test_dir):
                print(f'❌ Missing test directory: {test_dir}')
                sys.exit(1)
        
        # Check requirements files
        req_files = ['requirements.txt', 'tests/test_requirements.txt']
        for req_file in req_files:
            if not os.path.exists(req_file):
                print(f'❌ Missing requirements file: {req_file}')
                sys.exit(1)
        
        print('✅ Test configuration validated')
        "

  # Quick integration smoke test (optional, can fail without blocking)
  integration-smoke-test:
    runs-on: ubuntu-latest
    needs: test
    continue-on-error: true  # Don't block development on framework issues
    strategy:
      matrix:
        python-version: ['3.11', '3.13']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        allow-prereleases: true
    
    - name: Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r tests/test_requirements.txt
    
    - name: Quick integration test
      run: |
        echo "🔬 Running integration smoke test on Python ${{ matrix.python-version }}..."
        echo "⚠️ This test may fail due to framework documentation issues"
        echo "🎯 Goal: Catch obvious integration problems without blocking development"
        
        # Run a subset of integration tests that don't require full system health
        if python3 -m pytest tests/integration/test_api_schemas.py -v --tb=short; then
          echo "✅ Integration smoke test passed on Python ${{ matrix.python-version }}"
        else
          echo "⚠️ Integration smoke test failed on Python ${{ matrix.python-version }} (not blocking)"
        fi

  # Summary job
  ci-summary:
    runs-on: ubuntu-latest
    needs: [test, integration-smoke-test]
    if: always()
    
    steps:
    - name: CI Summary
      run: |
        echo "🏁 CI Summary for Development Velocity + Python 3.13 Support"
        echo "============================================================"
        
        # Check Python 3.11 results (required)
        python311_test_result="${{ needs.test.result }}"
        if [[ "$python311_test_result" == "success" ]]; then
          echo "✅ Python 3.11 Tests: PASSED (Required)"
        else
          echo "❌ Python 3.11 Tests: FAILED (Required)"
        fi
        
        # Check Python 3.13 results (future target)
        echo "🚀 Python 3.13 Tests: Checked (Future compatibility)"
        
        if [ "${{ needs.integration-smoke-test.result }}" == "success" ]; then
          echo "✅ Integration Smoke Tests: PASSED"
        else
          echo "⚠️ Integration Smoke Tests: FAILED (non-blocking)"
        fi
        
        echo ""
        echo "📊 Enhanced CI Goals:"
        echo "• Fast feedback (< 8 minutes with matrix)"
        echo "• Python 3.11 stability (required for merge)"
        echo "• Python 3.13 future compatibility (informational)"
        echo "• Catch import/syntax/compatibility errors"
        echo "• Validate license compliance"
        echo "• Maintain development velocity"
        echo ""
        
        if [[ "$python311_test_result" == "success" ]]; then
          echo "🎉 Development can continue - Python 3.11 core functionality validated!"
          echo "🚀 Python 3.13 compatibility status available in detailed logs"
        else
          echo "🚨 Python 3.11 tests failed - please fix before merging"
          exit 1
        fi 